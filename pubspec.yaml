name: itantra
description: "iTantra — Offline Indian Multilingual TTS/STT Neural Transceiver"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  permission_handler: ^11.3.1
  path_provider: ^2.1.3
  google_fonts: ^6.2.1
  flutter_spinkit: ^5.2.1
  sherpa_onnx: ^1.10.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
